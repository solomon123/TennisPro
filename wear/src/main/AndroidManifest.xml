<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!--
          Tells the Play Store / companion app that this watch APK is standalone
          enough to be installed on its own. Set false because the watch app is
          useless without the phone recording alongside it.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Receives phone messages when the watch app is not in the foreground, which
          is the normal case: the point of an out-call buzz is that you are playing,
          not looking at your wrist.

          Deliberately no android:permission="...BIND_WEARABLE_LISTENER" here — see
          the matching note on the phone's PhoneWearableListenerService. On a real
          device, requiring it can make Play Services itself fail to bind to this
          service, silently dropping every message. The wear:-scheme intent-filters
          below already scope delivery correctly.
        -->
        <service
            android:name=".WatchWearableListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/tennispro" />
            </intent-filter>
            <!-- Delivers the latched match-score DataItem, including on reconnect. -->
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/tennispro" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>
</manifest>
